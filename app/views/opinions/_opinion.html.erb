<li> 
     <div class="for-image"> <%= image_tag opinion.user.cover_image, style: "width: 50px; height: 50px" %> </div>
     <div class="recent-follower">
          <div class="person-time">
                <h5> <%= opinion.user.fullname %></h5>  
                <span class="opionion-time"> <%= opinion.created_at.strftime("%b %d, %I:%M %p") %></span> 
          </div>
          <div>  <%= opinion.comment %> </div>
     </div>
</li>